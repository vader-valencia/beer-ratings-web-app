import Carousel from "../Components/Carousel";

export default function Category() {
    return (
        <Carousel 
        images={[]} 
        isMobileStepperActive={true}        
        />
    )
}